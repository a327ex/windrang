# engine binaries — copies of Anchor/engine/engine/build/ (re-copy on engine updates;
# avcodec alone is 63MB and all are rebuildable)
anchor.exe
*.dll

# presentation-stream recordings (large; archive is the site vault)
replays/
render/
